<DocumentType js></DocumentType>

// En el navegador, apretar F12 para ver la Consola
console.log("Hola mundo")

// Con JQuery, todo lo que es inicializacion se hace dentro de este 'ready', que se llama cuando el documento esta listo
$(document).ready(function(){
    //Mi idea es obtener el URL y buscar la posición, en lo que sería su cadena de caracteres, del signo de pregunta,
    //que es a partir de ahí de donde se pueden agregar parámetros si mal no entiendo. Entonces,  
    function hacerURL(hora, lugar){
        var url = tostring(window.location.href);
        url
    }
});